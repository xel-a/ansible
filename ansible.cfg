[defaults]
host_key_checking = no
inventory = inventory
ask_pass = no
# remote_user = sysop
# remote_user = giyuu
remote_user = admin
timeout = 60
# private_key_file = ~/.ssh/ansible

# [privilege_escalation]
# become = yes
# become_method = sudo
# become_user = sysop
# become_user = root
# become_ask_pass = yes
